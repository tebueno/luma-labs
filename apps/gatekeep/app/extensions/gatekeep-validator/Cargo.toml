[package]
name = "gatekeep-validator"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "gatekeep-validator"
path = "src/main.rs"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[profile.release]
lto = true
opt-level = "z"
strip = true
codegen-units = 1
